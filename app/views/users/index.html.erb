

<% @users.each do |user| %>
<div class="user">

<%= image_tag(user.photo, alt: "user photo", class: "user_photo") %>

<div class="user_info">
	<h2><%= user.name %></h2>		
	<p>Number of posts: <%= user.posts_counter %></p>
</div>
	
	
	 

</div>
<% end %>
